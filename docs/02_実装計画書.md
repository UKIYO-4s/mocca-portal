# Moccaポータル 実装計画書

**バージョン:** 2.0
**最終更新:** 2025年12月29日

---

## 現在の完了状況

### ✅ Phase 1 完了済み
- Laravel Breeze インストール（React + TypeScript）
- MySQL 接続設定
- ユーザーモデルにroleカラム追加
- CheckRoleミドルウェア作成・登録
- テストユーザーSeeder作成・実行

### 📁 作成済みファイル
```
app/Models/User.php                           # roleカラム + ヘルパーメソッド
app/Http/Middleware/CheckRole.php             # ロールチェックミドルウェア
bootstrap/app.php                             # ミドルウェアエイリアス登録
database/migrations/xxxx_add_role_to_users.php # roleカラムマイグレーション
database/seeders/AdminUserSeeder.php          # テストユーザー
database/seeders/DatabaseSeeder.php           # Seeder呼び出し
resources/js/bootstrap.ts                     # axios設定
config/modules.php                            # モジュール設定（新規作成）
```

---

## Phase 2 実装計画

### 2.1 モジュールシステム基盤

#### 2.1.1 ディレクトリ作成
```bash
mkdir -p app/Contracts
mkdir -p app/Core/{Auth,User,Dashboard,Notification,ActivityLog,Location}/{Models,Controllers,Services}
mkdir -p app/Modules/{Reservation,Checklist,Inventory,TimeCard,Announcement,Shift}/{Models,Controllers,Services,Events,Widgets}
```

#### 2.1.2 インターフェース作成
**ファイル:** `app/Contracts/ModuleInterface.php`
```php
<?php

namespace App\Contracts;

interface ModuleInterface
{
    public function getName(): string;
    public function getVersion(): string;
    public function isEnabled(): bool;
    public function getDashboardWidgets(): array;
    public function getNavigation(): array;
    public function getPermissions(): array;
    public function boot(): void;
}
```

**ファイル:** `app/Contracts/DashboardWidgetInterface.php`
```php
<?php

namespace App\Contracts;

interface DashboardWidgetInterface
{
    public function getName(): string;
    public function getComponent(): string;
    public function getData(): array;
    public function canView(User $user): bool;
    public function getOrder(): int;
}
```

#### 2.1.3 基底モジュールクラス作成
**ファイル:** `app/Core/BaseModule.php`
```php
<?php

namespace App\Core;

use App\Contracts\ModuleInterface;

abstract class BaseModule implements ModuleInterface
{
    protected string $name;
    protected string $version = '1.0.0';

    public function getName(): string
    {
        return $this->name;
    }

    public function getVersion(): string
    {
        return $this->version;
    }

    public function isEnabled(): bool
    {
        return config("modules.enabled.{$this->name}", false);
    }

    public function getDashboardWidgets(): array
    {
        return [];
    }

    public function getNavigation(): array
    {
        return [];
    }

    public function getPermissions(): array
    {
        return [];
    }

    abstract public function boot(): void;
}
```

#### 2.1.4 モジュールサービスプロバイダ作成
**ファイル:** `app/Providers/ModuleServiceProvider.php`
```php
<?php

namespace App\Providers;

use Illuminate\Support\ServiceProvider;

class ModuleServiceProvider extends ServiceProvider
{
    protected array $modules = [
        'reservation' => \App\Modules\Reservation\ReservationModule::class,
        'checklist' => \App\Modules\Checklist\ChecklistModule::class,
        'inventory' => \App\Modules\Inventory\InventoryModule::class,
        'timecard' => \App\Modules\TimeCard\TimeCardModule::class,
        'announcement' => \App\Modules\Announcement\AnnouncementModule::class,
        'shift' => \App\Modules\Shift\ShiftModule::class,
    ];

    public function register(): void
    {
        foreach ($this->modules as $name => $class) {
            if (config("modules.enabled.{$name}", false) && class_exists($class)) {
                $this->app->singleton("module.{$name}", fn() => new $class());
            }
        }
    }

    public function boot(): void
    {
        foreach ($this->modules as $name => $class) {
            if (config("modules.enabled.{$name}", false) && class_exists($class)) {
                $module = app("module.{$name}");
                $module->boot();
            }
        }
    }
}
```

#### 2.1.5 プロバイダ登録
**ファイル:** `bootstrap/providers.php` に追加
```php
return [
    App\Providers\AppServiceProvider::class,
    App\Providers\ModuleServiceProvider::class,  // 追加
];
```

---

### 2.2 拠点マスター

#### 2.2.1 マイグレーション作成
```bash
php artisan make:migration create_locations_table
```

**内容:**
```php
Schema::create('locations', function (Blueprint $table) {
    $table->id();
    $table->string('name');
    $table->string('slug', 50)->unique();
    $table->boolean('is_active')->default(true);
    $table->timestamps();
});
```

#### 2.2.2 初期データSeeder
**ファイル:** `database/seeders/LocationSeeder.php`
```php
Location::updateOrCreate(['slug' => 'ajito'], ['name' => 'アジト']);
Location::updateOrCreate(['slug' => 'banshirou'], ['name' => 'ばんしろう']);
```

#### 2.2.3 モデル作成
```bash
php artisan make:model Core/Location/Models/Location
```

---

### 2.3 アクティビティログ

#### 2.3.1 マイグレーション作成
```bash
php artisan make:migration create_activity_logs_table
```

**内容:**
```php
Schema::create('activity_logs', function (Blueprint $table) {
    $table->id();
    $table->foreignId('user_id')->constrained()->onDelete('cascade');
    $table->string('module', 50);
    $table->string('action', 100);
    $table->string('target_type', 100)->nullable();
    $table->unsignedBigInteger('target_id')->nullable();
    $table->text('description')->nullable();
    $table->string('ip_address', 45)->nullable();
    $table->text('user_agent')->nullable();
    $table->timestamp('created_at');

    $table->index(['user_id', 'created_at']);
    $table->index(['module', 'action']);
});
```

#### 2.3.2 サービスクラス作成
**ファイル:** `app/Core/ActivityLog/Services/ActivityLogService.php`
```php
<?php

namespace App\Core\ActivityLog\Services;

use App\Core\ActivityLog\Models\ActivityLog;
use Illuminate\Support\Facades\Auth;
use Illuminate\Support\Facades\Request;

class ActivityLogService
{
    public function log(
        string $module,
        string $action,
        ?string $targetType = null,
        ?int $targetId = null,
        ?string $description = null
    ): ActivityLog {
        return ActivityLog::create([
            'user_id' => Auth::id(),
            'module' => $module,
            'action' => $action,
            'target_type' => $targetType,
            'target_id' => $targetId,
            'description' => $description,
            'ip_address' => Request::ip(),
            'user_agent' => Request::userAgent(),
        ]);
    }
}
```

---

### 2.4 通知システム

#### 2.4.1 Laravelの通知テーブル作成
```bash
php artisan make:notifications-table
php artisan migrate
```

#### 2.4.2 基本通知クラス
```bash
php artisan make:notification BaseNotification
```

---

### 2.5 ダッシュボードウィジェットシステム

#### 2.5.1 ウィジェットマネージャー作成
**ファイル:** `app/Core/Dashboard/Services/WidgetManager.php`
```php
<?php

namespace App\Core\Dashboard\Services;

use App\Contracts\DashboardWidgetInterface;
use App\Models\User;

class WidgetManager
{
    protected array $widgets = [];

    public function register(DashboardWidgetInterface $widget): void
    {
        $this->widgets[] = $widget;
    }

    public function getWidgetsForUser(User $user): array
    {
        return collect($this->widgets)
            ->filter(fn($widget) => $widget->canView($user))
            ->sortBy(fn($widget) => $widget->getOrder())
            ->map(fn($widget) => [
                'name' => $widget->getName(),
                'component' => $widget->getComponent(),
                'data' => $widget->getData(),
            ])
            ->values()
            ->toArray();
    }
}
```

#### 2.5.2 ダッシュボードコントローラー
**ファイル:** `app/Core/Dashboard/Controllers/DashboardController.php`
```php
<?php

namespace App\Core\Dashboard\Controllers;

use App\Http\Controllers\Controller;
use App\Core\Dashboard\Services\WidgetManager;
use Illuminate\Http\Request;
use Inertia\Inertia;

class DashboardController extends Controller
{
    public function __construct(
        protected WidgetManager $widgetManager
    ) {}

    public function index(Request $request)
    {
        $widgets = $this->widgetManager->getWidgetsForUser($request->user());

        return Inertia::render('Dashboard', [
            'widgets' => $widgets,
        ]);
    }
}
```

---

### 2.6 予約モジュール（ばんしろう）

#### 2.6.1 マイグレーション作成
```bash
php artisan make:migration create_banshirou_reservations_table
php artisan make:migration create_reservation_assignments_table
```

**banshirou_reservations:**
```php
Schema::create('banshirou_reservations', function (Blueprint $table) {
    $table->id();
    $table->string('name');
    $table->string('name_kana');
    $table->string('phone', 20);
    $table->string('email')->nullable();
    $table->text('address');
    $table->date('checkin_date');
    $table->date('checkout_date');
    $table->unsignedInteger('guest_count_adults')->default(1);
    $table->unsignedInteger('guest_count_children')->default(0);
    $table->enum('meal_option', ['with_meals', 'seat_only', 'no_meals']);
    $table->boolean('pickup_required')->default(false);
    $table->json('options')->nullable();
    $table->enum('payment_method', ['cash', 'credit', 'bank_transfer']);
    $table->text('notes')->nullable();
    $table->foreignId('created_by')->constrained('users')->onDelete('cascade');
    $table->enum('status', ['confirmed', 'cancelled'])->default('confirmed');
    $table->timestamps();

    $table->index('checkin_date');
    $table->index('checkout_date');
});
```

**reservation_assignments:**
```php
Schema::create('reservation_assignments', function (Blueprint $table) {
    $table->id();
    $table->foreignId('reservation_id')->constrained('banshirou_reservations')->onDelete('cascade');
    $table->enum('assignment_type', ['cleaning', 'setup']);
    $table->foreignId('user_id')->constrained()->onDelete('cascade');
    $table->boolean('reminder_sent_day_before')->default(false);
    $table->boolean('reminder_sent_day_of')->default(false);
    $table->timestamps();

    $table->unique(['reservation_id', 'assignment_type']);
});
```

#### 2.6.2 モデル作成
```bash
php artisan make:model Modules/Reservation/Models/BanshirouReservation
php artisan make:model Modules/Reservation/Models/ReservationAssignment
```

#### 2.6.3 コントローラー作成
```bash
php artisan make:controller Modules/Reservation/Controllers/BanshirouReservationController --resource
```

#### 2.6.4 Reactコンポーネント作成
```
resources/js/Pages/Reservations/
├── Index.tsx              # 予約一覧
├── Create/
│   ├── Index.tsx          # ステップフォーム親
│   ├── Step1Customer.tsx  # お客様情報
│   ├── Step2Stay.tsx      # 宿泊情報
│   ├── Step3Meal.tsx      # お食事・オプション
│   ├── Step4Payment.tsx   # お支払い・備考
│   └── Step5Confirm.tsx   # 確認画面
├── Show.tsx               # 予約詳細
└── Edit.tsx               # 予約編集
```

---

### 2.7 予約モジュール（もっか）

#### 2.7.1 マイグレーション作成
```bash
php artisan make:migration create_mocca_reservations_table
```

**内容:**
```php
Schema::create('mocca_reservations', function (Blueprint $table) {
    $table->id();
    $table->enum('reservation_type', ['breakfast', 'lunch', 'dinner']);
    $table->date('reservation_date');
    $table->string('name');
    $table->unsignedInteger('guest_count');
    $table->time('arrival_time')->nullable();
    $table->string('phone', 20)->nullable();
    $table->text('advance_menu')->nullable();
    $table->text('notes')->nullable();
    $table->foreignId('banshirou_reservation_id')
          ->nullable()
          ->constrained('banshirou_reservations')
          ->nullOnDelete();
    $table->foreignId('created_by')->constrained('users')->onDelete('cascade');
    $table->enum('status', ['confirmed', 'cancelled'])->default('confirmed');
    $table->timestamps();

    $table->index(['reservation_date', 'reservation_type']);
});
```

---

### 2.8 ユーザー招待システム

#### 2.8.1 マイグレーション作成
```bash
php artisan make:migration create_user_invitations_table
```

**内容:**
```php
Schema::create('user_invitations', function (Blueprint $table) {
    $table->id();
    $table->string('email');
    $table->string('token', 64)->unique();
    $table->foreignId('invited_by')->constrained('users')->onDelete('cascade');
    $table->timestamp('expires_at');
    $table->timestamp('accepted_at')->nullable();
    $table->timestamp('created_at');
});
```

---

## 実装順序チェックリスト

### Step 1: モジュール基盤 ⬜
- [ ] ディレクトリ構造作成
- [ ] ModuleInterface作成
- [ ] DashboardWidgetInterface作成
- [ ] BaseModule作成
- [ ] ModuleServiceProvider作成・登録

### Step 2: コアテーブル ⬜
- [ ] locationsマイグレーション作成・実行
- [ ] Locationモデル作成
- [ ] LocationSeeder作成・実行
- [ ] activity_logsマイグレーション作成・実行
- [ ] ActivityLogモデル作成
- [ ] ActivityLogService作成
- [ ] user_invitationsマイグレーション作成・実行
- [ ] UserInvitationモデル作成

### Step 3: 通知・ダッシュボード ⬜
- [ ] 通知テーブル作成
- [ ] WidgetManager作成
- [ ] DashboardController作成
- [ ] Dashboard.tsxコンポーネント更新

### Step 4: 予約モジュール（ばんしろう） ⬜
- [ ] banshirou_reservationsマイグレーション作成・実行
- [ ] reservation_assignmentsマイグレーション作成・実行
- [ ] BanshirouReservationモデル作成
- [ ] ReservationAssignmentモデル作成
- [ ] BanshirouReservationController作成
- [ ] ルート定義
- [ ] Reactコンポーネント作成（Index, Create/Steps, Show, Edit）
- [ ] ReservationModuleクラス作成

### Step 5: 予約モジュール（もっか） ⬜
- [ ] mocca_reservationsマイグレーション作成・実行
- [ ] MoccaReservationモデル作成
- [ ] MoccaReservationController作成
- [ ] Reactコンポーネント作成

### Step 6: 担当割り当て・リマインド ⬜
- [ ] 担当割り当てUI作成
- [ ] リマインドメール通知クラス作成
- [ ] スケジューラ設定（前日・当日通知）

---

## コマンドリファレンス

### 開発サーバー起動
```bash
# Laravelサーバー
php artisan serve

# Vite開発サーバー
npm run dev
```

### マイグレーション
```bash
# マイグレーション実行
php artisan migrate

# ロールバック
php artisan migrate:rollback

# リフレッシュ（全削除＋再実行）
php artisan migrate:refresh --seed
```

### Seeder
```bash
# 全Seeder実行
php artisan db:seed

# 特定Seeder実行
php artisan db:seed --class=AdminUserSeeder
```

### キャッシュクリア
```bash
php artisan config:clear
php artisan cache:clear
php artisan route:clear
php artisan view:clear
```

---

## 注意事項

1. **中断時の対応**: このドキュメントのチェックリストを確認し、未完了の項目から再開する
2. **マイグレーション順序**: 外部キー制約があるため、親テーブルを先に作成する
3. **モジュール無効化**: `config/modules.php`でfalseに設定するだけで機能を無効化できる
4. **テスト用アカウント**:
   - admin@mocca-portal.local / password
   - manager@mocca-portal.local / password
   - staff@mocca-portal.local / password
